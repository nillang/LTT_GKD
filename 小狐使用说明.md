# 小狐 使用说明

> 小狐 v1.0.0 · 广告自动跳过助手
>
> 小狐通过 Android 无障碍服务监听应用窗口变化，自动识别并点击开屏广告、插屏弹窗等场景中的"跳过""关闭"按钮。**所有识别与点击均在本地完成，不上传任何用户数据。**

---

## 目录

1. [功能简介](#1-功能简介)
2. [首次使用](#2-首次使用)
3. [首页](#3-首页)
4. [规则管理](#4-规则管理)
5. [新建与编辑规则](#5-新建与编辑规则)
6. [订阅规则](#6-订阅规则)
7. [分享规则](#7-分享规则)
8. [应用白名单](#8-应用白名单)
9. [跳过记录](#9-跳过记录)
10. [日志查看](#10-日志查看)
11. [设置详解](#11-设置详解)
12. [后台保活指南](#12-后台保活指南)
13. [常见问题](#13-常见问题)

---

## 1. 功能简介

| 能力 | 说明 |
| --- | --- |
| 自动跳广告 | 开屏广告、插屏弹窗、Banner 横幅、激励视频，自动点击"跳过/关闭" |
| 三类规则 | **内置规则**（随 APP 提供）、**订阅规则**（在线同步）、**本地规则**（自己编写） |
| 官方规则库 | 首次启动自动订阅官方源，覆盖主流应用，开箱即用 |
| OCR 兜底 | 控件树找不到按钮文字时，截图 OCR 识别坐标点击（应对纯图片广告） |
| 规则分享 | 本地规则可上传到 GitHub Gist 分享给他人，也可订阅他人分享的规则 |
| 跳过统计 | 累计/今日/本周/本月跳过次数与近 7 天趋势图 |
| 应用白名单 | 指定不执行跳过的应用 |
| 后台保活 | 前台服务 + 看门狗通知 + 保活引导，降低被系统杀死的概率 |

---

## 2. 首次使用

### 第一步：安装并打开小狐

安装后桌面会出现"小狐"图标（青色背景的狐狸图标），点击打开。

### 第二步：开启无障碍服务（必须）

小狐的核心能力依赖无障碍服务，**必须手动开启**：

1. 在首页点击中间的大电源按钮（或进入「设置 → 基础设置 → 无障碍服务」）；
2. 系统会跳转到**无障碍设置页**；
3. 在列表中找到 **"小狐 - 广告自动跳过"**，点击进入并开启开关；
4. 系统弹出确认对话框时点击"允许"。

开启成功后返回小狐，电源按钮变为青色实心并带脉冲动画，显示"运行中"。

> 关闭服务：同样点击电源按钮跳到系统设置页，关闭开关即可。

### 第三步：授予通知权限（建议）

Android 13+ 首次开启服务时会请求通知权限，建议允许，用于：

- 每次成功跳过时弹出提示（可在设置中关闭）；
- 服务被系统杀死时，看门狗发送"服务未运行"通知提醒重新开启；
- 前台常驻静默通知（"小狐运行中"），用于保活。

### 第四步：允许读取应用列表（OPPO/ColorOS 必须手动操作）

小狐需要读取已安装应用列表，才能让对应应用的规则生效，并提供白名单功能。

- **OPPO / 一加 / realme（ColorOS）**：安装后需手动授权，路径为
  **设置 → 应用管理 → 小狐 → 权限管理 → 读取应用列表**，设为"允许"
  （部分 ColorOS 版本在 **手机管家 → 应用权限管理** 中）。
- 其他品牌机型通常默认允许，无需操作。

### 第五步：完成保活设置（强烈建议）

国产 ROM 容易回收后台服务，请参照 [第 12 节：后台保活指南](#12-后台保活指南) 完成设置，避免偶发漏跳。

---

## 3. 首页

首页是小狐的主控台：

- **中部电源按钮**：显示服务状态。
  - 青色实心圆 + 脉冲环 = 服务运行中；
  - 白底灰边圆 = 服务已关闭；
  - 点击按钮跳转到系统无障碍设置页进行开启/关闭。
- **状态文字**："服务已开启，自动跳过广告中" / "点击开启无障碍服务"。
- **底部累计跳过**：显示历史累计跳过广告的总次数（千分位格式）。
  - **查看**：进入[跳过记录页](#9-跳过记录)（仅服务开启时可用）；
  - **重置**：将累计次数清零。

底部导航栏有三个页面：**首页 / 规则 / 设置**。

---

## 4. 规则管理

点击底部导航的「规则」进入规则管理页，顶部有三个标签页：

### 4.1 本地

你自己创建或导入的规则。

- **新建规则**：点击右下角蓝色 **＋** 按钮，进入规则编辑器（详见[第 5 节](#5-新建与编辑规则)）。
- **启用 / 禁用**：点击卡片右侧开关。
- **编辑**：点击规则卡片进入编辑页。
- **删除**：点击卡片上的垃圾桶图标，确认后删除。
- **导入 / 导出**：点击右上角 **⋮（三点菜单）**：
  - **导入规则**：从手机中选择 JSON 规则文件导入（仅支持 JSON 格式）；
  - **粘贴 JSON 导入**：直接粘贴别人分享的规则 JSON 文本导入；
  - **导出规则**：把全部本地规则打包为 `ltt_rules_export.json` 保存，方便换机或分享。
- 已上传分享的规则会显示蓝色 **"已分享"** 徽章；订阅同步回来后还会显示使用人数。

### 4.2 订阅

从网络同步的规则（默认已包含"官方规则库"）。页面分为两部分：

**上方：订阅源管理**

- **＋ 按钮**（右下角）：添加订阅源，支持
  - GitHub Gist 链接或 32 位 Gist ID；
  - 任意规则 JSON 直链（如 jsDelivr 链接）。
  - 填写名称（可选）和链接后点击"添加并同步"，会立即拉取一次。
- 每个订阅源可：**单独同步**（刷新图标）、**启用/禁用**（开关）、**删除**（垃圾桶图标，会同时删除该源的规则文件）。
- **全部同步**：一键同步所有已启用的订阅源，同步结果以 Toast 提示：
  - "全部同步成功：N 个源"
  - "同步完成：成功 x/y 个源"
  - "同步失败，请检查网络与订阅链接"

**下方：订阅规则列表**

- 卡片上显示规则名、包名、优先级、作者、使用量；
- 使用量达到 10 人的规则带橙色 **"热门"** 徽章；
- 订阅规则不可编辑、删除，但可以单独启用/禁用。

**未安装应用折叠区**

列表底部有"未安装应用 (N 条)"折叠区：对应应用尚未安装的规则暂不生效，安装该应用后重新打开小狐（主界面会自动刷新），规则即自动激活。

### 4.3 内置

随 APP 安装包自带的规则，按应用类别分为多个合集（如社交、视频、购物、金融、工具等）：

- **合集总开关**：一键启用/禁用整个合集，头部显示"已启用 x / y 条"；
- **单条开关**：可单独控制合集内的每条规则；
- **预览原文**：点击合集头部的文件图标，可查看该合集的 JSON 原文；
- 内置规则只显示**已安装应用**的规则和**通用兜底规则**（包名为空），未安装应用的规则自动隐藏；
- 内置规则不可编辑、删除。

> 规则生效顺序：按优先级从高到低匹配。开屏（100）> 弹窗（80）> 激励视频（70）> Banner（60）> 通用兜底（1）。

---

## 5. 新建与编辑规则

在「规则 → 本地」页点右下角 **＋** 进入新建规则页。

### 5.1 第一步：选择广告类型（模板）

新建时先选择场景模板，选中后会自动填充推荐参数（优先级、节流、关键词、动作），之后仍可自由修改：

| 模板 | 适用场景 | 推荐优先级 | 节流 |
| --- | --- | --- | --- |
| **开屏广告** | App 启动时全屏 3–5 秒，"跳过"按钮在右上/右下角 | 100 | 5 秒 |
| **插屏弹窗** | 页面切换时弹出的广告，"×"关闭按钮常在右上角 | 80 | 3 秒 |
| **Banner 横幅** | 页面顶部/底部条状广告，关闭入口藏在小三角里 | 60 | 3 秒 |
| **激励视频** | 看广告换奖励，倒计时结束才出现关闭按钮 | 70 | 30 秒 |
| **通用兜底** | 跨 App 的关键词匹配，最后防线 | 1 | 5 秒 |

### 5.2 填写基本信息

- **规则 ID**：全局唯一，建议格式 `包名_场景`，例如 `com.taobao.taobao_splash`（选择模板并填包名后会自动建议）。
- **规则名称**：便于识别的名字，例如"淘宝开屏"。
- **包名**：目标应用的包名；**留空表示通用兜底规则**（对所有应用生效）。
  - 不知道包名？可到「设置 → 应用白名单」里点击/长按应用行，即可复制包名。
- **启用**：保存后是否立即生效。

### 5.3 匹配与动作

- **匹配文本**：要查找的关键词，多个用英文逗号分隔，如 `跳过,跳过广告,Skip`。
- **处理方式（动作）**：
  - **点击节点**：点击命中的按钮本身（最常用）；
  - **点击坐标**：点击按钮中心位置（适合不可点击的控件，如部分 Banner）；
  - **返回**：模拟按返回键（适合弹窗类广告）；
  - **手势点击**：通过手势在 OCR 识别出的坐标处点击。
- **忽略大小写**：默认开启，"Skip"与"skip"视为相同。

### 5.4 高级选项（折叠区）

点击"高级选项"展开：

- **匹配类型**：
  - **TEXT**：匹配按钮文字或内容描述（默认）；
  - **DESC**：仅匹配内容描述（contentDescription，弹窗关闭按钮常用）；
  - **ID**：按 View ID 匹配（需在 ViewID 栏填写，如 `com.xxx:id/skip_btn`）；
  - **OCR**：截图识别文字（由 OCR 兜底通道处理）。
- **ViewID**：type 为 ID 时填写，多个用逗号分隔。
- **正则匹配**：把匹配文本当作正则表达式。
- **Activity 限定**：只在指定页面生效，留空表示任意页面。
- **优先级**：数字越大越优先。
- **节流（毫秒）**：同一条规则两次触发的最小间隔，防止短时间重复点击。
- **动作延迟（毫秒）**：命中后等待多久再点击，0 表示立即。

### 5.5 保存

- 底部点 **"保存本地"** 即可。保存前会自动校验 ID、名称、包名格式、匹配文本等，不合法时会用 Toast 提示原因。
- 编辑已有规则后保存，会保留"已分享"标记与作者信息。

---

## 6. 订阅规则

订阅功能让你不用逐条手写规则，直接使用他人维护的规则集，并保持更新。

### 6.1 官方规则库

APP 首次启动会**自动添加并同步官方规则库**（通过 jsDelivr CDN 分发，国内有加速节点），主流应用的广告规则开箱即用。你可以在「规则 → 订阅」页看到它。

### 6.2 添加自己的订阅源

1. 进入「规则 → 订阅」，点右下角 **＋**；
2. 名称随意填（如"朋友的规则"），链接填入对方给你的 **Gist 链接 / Gist ID / JSON 直链**；
3. 点 **"添加并同步"**，成功后 Toast 提示"添加成功，同步 N 条规则"。

### 6.3 自动更新

1. 进入「设置 → 规则订阅（自动更新）」卡片，点击展开；
2. 打开 **"自动更新"** 开关；
3. 填写 **更新间隔（小时）**，须为正整数（默认 24 小时）。

开启后，系统会按间隔在后台同步所有"已启用"的订阅源。订阅源本身的添加、删除、开关管理统一在「规则 → 订阅」页进行。

> 订阅规则只显示已安装应用对应的规则和通用兜底规则；未安装应用的规则在折叠区待命。

---

## 7. 分享规则

你编写的本地规则可以上传到 GitHub Gist 分享给他人（他人通过订阅你的 Gist 链接即可使用）。

### 7.1 配置 GitHub 凭据（仅首次）

1. 到 GitHub 官网申请一个 **Personal Access Token（经典）**，勾选 `gist` 权限；
2. 打开小狐「设置 → 贡献者」卡片，展开；
3. 在 **GitHub Token** 栏粘贴 Token（输入内容自动掩码显示）；
4. **Gist ID** 不用填，首次上传成功后会自动生成并回填。

> **安全说明**：Token 使用 Android Keystore + AES-GCM 加密后仅保存在本机；上传的 Gist 设为**私有**，但持有链接者均可订阅。

### 7.2 上传规则

1. 在规则编辑器编辑好规则后，点底部 **"上传"** 按钮；
2. APP 会先检查社区中是否已存在相同 ID 的规则：
   - 若该 ID 已被别人上传，会提示 **"规则已存在，只保留第一个创建者的版本"** 并终止上传；
   - 上传成功后，本地规则出现蓝色"已分享"徽章。
3. 把你的 **Gist 链接**发给朋友，对方按[第 6.2 节](#62-添加自己的订阅源)添加订阅即可。
4. 之后有人订阅使用，规则卡片上会显示"已被 N 人使用 · 感谢分享"。

> 若 Token 曾在聊天、截图等场合泄露，请立即到 GitHub 设置中**吊销（Revoke）并重新生成**。

---

## 8. 应用白名单

白名单用于指定**不对哪些应用执行跳广告**（系统应用默认加入白名单）。

入口：「设置 → 基础设置 → 应用白名单」。

- 顶部搜索框：按应用名或包名搜索（输入后约 250ms 自动搜索）；
- 筛选标签：**全部 / 已白名单 / 待跳过**；
- 每行显示应用图标、名称、包名、版本，右侧为白名单开关：
  - **开关打开（加入白名单）**：小狐不会处理该应用的任何广告；
  - **开关关闭**：正常对该应用执行规则；
- **点击/长按应用行**：复制包名到剪贴板（编写规则时用）；
- 右上角 **重置**：恢复默认白名单（系统应用白名单、用户应用全部待跳过），需确认。

---

## 9. 跳过记录

首页点 **"查看"**（服务开启时可用）进入跳过记录页：

- 顶部大数字：**累计跳过**次数；
- 三个统计：**今日 / 本周 / 本月**跳过次数；
- **近 7 天趋势图**：圆角柱状图，带中线基准，每根柱子上方显示次数、下方显示日期；
- **最近跳过列表**：按时间倒序显示每次跳过的应用、时间等信息；
- 右上角**清空按钮**：清空历史记录列表（累计跳过次数不受影响），需确认。

---

## 10. 日志查看

入口：「设置 → 基础设置 → 日志」。

日志记录每次规则匹配与点击行为，是排查"为什么没跳过/误跳过"的主要工具。

- **刷新**：手动刷新日志；页面回到前台时也会自动刷新，查看时约每 2 秒自动刷新；
- **清空**：清空内存日志缓冲并删除所有历史日志文件（需确认）；
- **历史日志文件**：页面下方以列表展示历史日志文件（区域限高，可滚动）；
- **导出/分享**：可通过系统分享把日志文件发送出来，便于反馈问题。

---

## 11. 设置详解

「设置」页从上到下依次为：

### 11.1 红色警示横幅

仅当无障碍服务未运行时出现，提示"广告跳过服务未运行"，点击横幅直接前往系统设置开启。

### 11.2 基础设置

| 项目 | 说明 |
| --- | --- |
| **无障碍服务** | 显示运行状态，点击跳转系统无障碍设置页 |
| **OCR 兜底** | 默认开启。控件树找不到"跳过"文字时，自动截图 OCR 识别并点击，可应对纯图片开屏广告 |
| **跳过通知** | 默认关闭。开启后每次成功跳过都在通知栏提示"已自动跳过 xx 广告" |
| **日志** | 进入[日志查看页](#10-日志查看)（记录始终开启） |
| **应用白名单** | 进入[白名单管理页](#8-应用白名单) |

### 11.3 后台保活

静态引导卡片，列出三项建议（详见下一节）。

### 11.4 规则订阅（自动更新）

折叠卡片，设置订阅规则的后台自动更新开关与间隔；蓝色横幅提示订阅源请到「规则 → 订阅」页管理。

### 11.5 贡献者

折叠卡片，包含：

- **设备 ID**：上传规则时自动生成的本机标识；
- **GitHub Token / Gist ID**：用于[分享规则](#7-分享规则)；
- 底部灰色安全说明：加密存储、Gist 私有、仅本地保存。

### 11.6 关于

显示版本号：**小狐 v1.0.0**。

---

## 12. 后台保活指南

Android 国产 ROM（尤其 OPPO/ColorOS）会周期性清理后台进程，导致服务中断、广告漏跳。服务被杀死后，小狐会发出"小狐服务未运行"的看门狗通知，点击可重新开启。要减少被杀，请完成以下三项设置：

1. **最近任务加锁**：打开小狐后切到最近任务界面，把小狐卡片**下拉锁定**（出现锁图标），防止"一键清理"时被划掉。
2. **允许后台活动**：在系统电池/电源管理中，把小狐设为**"允许后台活动"**或**"无限制"**（允许后台耗电）。
3. **允许自启动**：
   - ColorOS：**设置 → 应用管理 → 小狐 → 自动启动**，允许；
   - 其他品牌：在"自启动管理 / 应用启动管理"中允许小狐自启动和关联启动。

其他补充建议：

- 不要把小狐安装到 SD 卡或"应用分身/隐私空间"中；
- 系统提示"检测到高耗电"时选择**保持运行**，不要一键优化；
- 小狐以前台服务方式运行，通知栏会有一条低重要性的静默通知"小狐运行中"，**请勿上滑清除**，这是保活所必需的。

### 异常退出提示

如果上次小狐被系统终止或发生崩溃，下次打开会弹出"小狐上次异常退出"提示，点"去开启"可直接跳到无障碍设置页重新打开服务。

---

## 13. 常见问题

**Q：开启了服务，但广告没有被跳过？**

按以下顺序排查：

1. 确认首页电源按钮显示"运行中"；设置页没有红色横幅；
2. 到「规则 → 内置/订阅」确认该应用的规则存在且开关已打开；
3. 如果规则在"未安装应用"折叠区，说明系统没读到应用列表，请授予"读取应用列表"权限（见[第 2 节](#第二步允许读取应用列表oppocoloros-必须手动操作)），然后回到小狐主界面等待自动刷新；
4. 确认该应用不在白名单中（「设置 → 应用白名单」）；
5. 打开「日志」页复现一次，看是否有匹配记录——有匹配但没点击，多为动作类型不适配，可尝试把规则动作改为"点击坐标"；完全无匹配则是关键词/控件问题，可开启 **OCR 兜底**再试。

**Q：纯图片广告（没有"跳过"文字控件）能跳过吗？**

可以。保持「设置 → OCR 兜底」开启，控件树识别失败时会截图做 OCR 识别并按坐标手势点击。切应用时 OCR 节流会自动重置，不影响冷启动广告识别。

**Q：会不会误点广告内容或造成隐私泄露？**

- 小狐只点击规则匹配到的"跳过/关闭"目标，且同规则有节流间隔保护，不会乱点；
- 所有处理在本机完成，不需要账号注册，不收集、不上传任何使用数据；网络权限仅用于同步订阅规则。

**Q：换手机了，规则怎么办？**

在旧手机「规则 → 本地 → ⋮ → 导出规则」保存 JSON 文件，发到新手机后用"导入规则"或"粘贴 JSON 导入"恢复即可。

**Q：订阅同步失败？**

- 检查网络连接；
- 确认订阅链接是公开可访问的（GitHub 仓库必须为 **public**，私有仓库的 jsDelivr 链接会返回 404）；
- Gist 链接确认未被删除；
- 单源点刷新图标可看到具体失败原因（Toast 提示）。

**Q：通知栏一直有"小狐运行中"通知，能关掉吗？**

这是 Android 前台服务保活所必需的常驻通知（低重要性、静默、不发声音）。不建议清除或强制停止，否则服务会被系统回收。可以在系统通知设置中把该通知渠道设为"静默/不显示图标"，但不要关闭渠道本身。

**Q：ColorOS 上服务经常自己关闭？**

这是 ColorOS 的后台管控机制，无障碍服务可能被移出已启用列表。请完成[第 12 节](#12-后台保活指南)的三项保活设置；看到"小狐服务未运行"通知时点击即可重新开启。

**Q：内置规则和订阅规则冲突时听谁的？**

规则按**优先级从高到低**依次匹配，与来源无关；同一场景建议只启用一个来源的规则，或通过开关临时禁用其中一条来对比效果。

**Q：通用兜底规则是什么？**

包名为空的规则，对所有应用生效，优先级最低（1），作为最后的防线——当某应用没有专属规则时，靠"跳过/Skip"等通用关键词尝试点击。可在规则编辑器用"通用兜底"模板创建。
